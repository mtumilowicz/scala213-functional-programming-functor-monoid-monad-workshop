package workshop

object ForComprehensionWorkshop {

  def evenCombinations(ints: List[Int], chars: List[Char], strings: List[String]): List[String] = null
  // for comprehension
  // ints if even
  // chars
  // strings
  // yield, string interpolation

}
